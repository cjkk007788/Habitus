version: '3.8'
services:
  db:
    image: postgres:16
    environment:
      POSTGRES_USER: habitus
      POSTGRES_PASSWORD: habitus_dev
      POSTGRES_DB: habitus
    ports:
      - "5432:5432"
    volumes:
      - habitus_pgdata:/var/lib/postgresql/data

volumes:
  habitus_pgdata:
